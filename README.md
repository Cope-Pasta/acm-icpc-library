# acm-icpc-library

Before the first run `pre-commit install` for automatically building PDF on each commit.

My code library (a.k.a. reference document) used in ACM-ICPC.

Execute command `make` to generate the target pdf file.
